name: Container Release

on:
  push:
    tags:
      - "v*.*.*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@adfc1cffed6530d6453c9dbb40be5f4c5884b8a2 # genefoundry-router v0.9.1
