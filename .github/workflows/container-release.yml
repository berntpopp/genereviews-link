name: Container Release

on:
  push:
    tags:
      - "v*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@33ea508d8ba92538b151340d50a4dbfd680d7482 # genefoundry-router container-release standard v1
