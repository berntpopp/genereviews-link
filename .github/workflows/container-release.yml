name: Container Release

on:
  push:
    tags:
      - "v*.*.*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@3d3cc20477828ddbd8a0c980b5b4f709e2612c02 # genefoundry-router v0.8.6
