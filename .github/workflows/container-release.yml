name: Container Release

on:
  push:
    tags:
      - "v*.*.*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@6568a0ad7d68925440aba550a2a678282ea5bb6b # genefoundry-router v0.8.7
