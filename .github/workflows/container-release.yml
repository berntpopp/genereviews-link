name: Container Release

on:
  push:
    tags:
      - "v*.*.*"

permissions: {}

jobs:
  container-release:
    permissions:
      attestations: write
      contents: write
      id-token: write
      packages: write
    uses: berntpopp/genefoundry-router/.github/workflows/_container-release.yml@31ea81cee5475fc3655c047c63a89739948f99a9 # genefoundry-router v0.8.5
